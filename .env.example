# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret
JWT_SECRET=interndisha_super_secret_jwt_key_2025_empowerment

# Database Configuration (MySQL)
# Default runs in resilient auto-fallback mode with auto-seeding if MySQL credentials are empty or offline
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=interndisha_db

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173
